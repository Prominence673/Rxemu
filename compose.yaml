services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev

    container_name: music-cli-dev

    volumes:
      - .:/app

    working_dir: /app

    stdin_open: true
    tty: true
